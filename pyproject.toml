[project]
name = "one-piece-agents"
version = "0.0.0"
description = "Cursor SDK scripts for one-piece agent automation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "cursor-sdk>=0.1.0",
]

[project.scripts]
one-piece-agent = "scripts.agent_prompt:main"

[tool.uv]
dev-dependencies = []

[tool.ruff]
line-length = 100
