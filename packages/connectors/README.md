# Connectors

Reverse-ingestion connectors for one-piece.

```bash
cd packages/connectors
python -m one_piece_connectors.cli \
  --source ssos \
  --path /path/to/space_station_os \
  --output /tmp/ssos-graph.json
```
