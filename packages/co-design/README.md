# Co-Design

Autonomous co-design loop orchestrator for one-piece.

```bash
cd packages/co-design
python -m one_piece_codesign.cli \
  --program ../../tmp/program.json \
  --output ../../tmp/program-out.json \
  --db ../../tmp/codesign.db
```
