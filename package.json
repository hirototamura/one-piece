{
  "name": "one-piece",
  "private": true,
  "version": "0.0.0",
  "description": "Systems engineering SaaS PoC for hardware products",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "rm -rf packages/*/dist apps/*/dist"
  },
  "engines": {
    "node": ">=20"
  }
}
